# comment can have
#
# empty lines as part of it, which should be preserved
# however the empty lines between comments are not considered
# essential and will be collapsed when writing back
value = "foo"

# Writeback test-only automatically added JSON representation:
# {
#     "": {
#         "value": "foo"
#     }
# }
